# ============================================================================
# 10. 限流 — IP / 全局 / 路径限流（定时器热加载）
# ============================================================================
[rate_limit]
ip_rps = 0
ip_burst = 0
global_rps = 0
max_buckets = 100000
snapshot_interval_sec = 30
snapshot_path = ./rate_limit.bin
