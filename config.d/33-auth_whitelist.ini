# ============================================================================
# 8. 安全 — 免鉴权白名单（SIGHUP 热加载，不停机）
# ============================================================================
# exact path:  path = /api/health
# prefix path: path = /api/public/
# service:     zebra-public
# ============================================================================
[auth_whitelist]
path = /api/health
path = /api/build
path = /api/redis
path = /api/mysql
path = /api/combo
