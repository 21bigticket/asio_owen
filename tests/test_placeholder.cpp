#include <gtest/gtest.h>

// Placeholder — real tests land in M5 (test_mysql_pool.cpp / test_redis_pool.cpp).
// This exists only to verify the GoogleTest harness works end-to-end.

TEST(Placeholder, HarnessWorks) {
    EXPECT_EQ(1 + 1, 2);
}
