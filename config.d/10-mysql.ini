# ============================================================================
# 2. 数据库 — MySQL 连接池（重启生效）
# ============================================================================
[mysql]
host = 127.0.0.1
port = 3306
user = root
pass = Klzz_20200528
db = zebra_config
min_size = 8
max_size = 64
max_idle_sec = 60
connect_timeout_ms = 1000
read_timeout_ms = 500
keepalive_sec = 30
worker_threads = 32
