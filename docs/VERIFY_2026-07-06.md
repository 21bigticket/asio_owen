# 全量验证报告

> 验证日期：2026-07-06
> 验证范围：修复与重构后的功能压测、连接复用、内存检测
> 对应变更：CIDR 修复、SecurityRules 数据竞争、RS256 JWT exp/nbf、配置加载路径、main.cpp 拆分、OpenSSL 3 deprecation 消除

## 功能压测

**环境：** VM 192.168.139.230，wrk VM 本地，30t/100c，30s × 2 轮

| 接口 | #1 RPS | #2 RPS | 平均 RPS | avg_lat | Socket Errors |
|:----|:------:|:------:|:--------:|:-------:|:-------------:|
| Health | 138,400 | 134,454 | **136,427** | 0.66ms | 0 |
| Redis | 34,057 | 32,557 | **33,307** | 2.73ms | 0 |
| MySQL | 23,019 | 21,351 | **22,185** | 10.22ms | 0 |
| Config 直连 | 16,218 | 16,608 | **16,413** | 6.05ms | 0 |
| Config 网关 | 10,669 | 11,187 | **10,928** | 8.46ms | 0 |

## 三向日志闭环

| 维度 | error | warn | timeout | panic |
|:----|:----:|:----:|:-------:|:-----:|
| 客户端 wrk Socket Errors | — | — | 0 | — |
| 上游 zebra-config（压测期间） | 0 | 0 | 0 | 0 |
| ASIO 网关 | 0 | 0 | 0 | 0 |

## 连接复用

**方法：** tcpdump 抓纯 SYN `tcp dst port 30001 and (tcp[13] & 0x12 == 0x02)`，15s 压测

| 指标 | 值 | 判定 |
|:----|:--:|:----:|
| 纯 SYN（新建连接） | 500 | — |
| 总请求 | ~120,060（15s × 8,004 RPS） | — |
| **复用率** | **(120,060 - 500) / 120,060 ≈ 99.6%** | ✅ |
| TIME_WAIT | **0** | ✅ 无堆积 |
| ESTAB | 83 | ✅ 稳定 |

## 内存检测

### ASAN

| 阶段 | VmRSS |
|:----|:-----:|
| 初始 | 142,348 kB |
| Health 30s | 580,364 kB |
| Redis 30s | 646,724 kB |
| MySQL 30s | 855,628 kB |
| Config 网关 30s | 975,672 kB |
| VmHWM（峰值） | 1,052,888 kB |

ASAN 全程零运行时错误，LSAN 无泄漏输出。RSS 增长是 ASAN 自身 overhead。

### Valgrind

```
LEAK SUMMARY:
    definitely lost: 0 bytes in 0 blocks    ✅
    indirectly lost: 0 bytes in 0 blocks    ✅
      possibly lost: 200 bytes in 2 blocks  ← Valgrind stack 指向 libmysqlclient 初始化/进程级状态
    still reachable: 30,290 bytes in 587 blocks  ← 正常
ERROR SUMMARY: 2 errors from 2 contexts     ← Valgrind 将 possibly lost 计入 error summary
```

**程序自身可控资源未发现泄漏。** `definitely lost` / `indirectly lost` 均为 0；MySQL 连接由 `mysql_close()` 释放，查询结果由 `mysql_free_result()` 释放。200 bytes `possibly lost` 的 Valgrind 栈若确认落在 libmysqlclient 初始化或进程级内部状态路径，可按第三方库噪声处理，必要时用 suppression 屏蔽。若需要继续追求 Valgrind 零噪声，可补进程级 `mysql_library_init()` / `mysql_library_end()` 生命周期封装后复测。

### 普通版本 RSS

压测后 VmRSS ≈ **120 MB**，VmHWM ≈ **123 MB**，无增长趋势。

## 编译

- `cmake -B build_codex_verify -S .` — 通过
- `cmake --build build_codex_verify` — 通过，0 warnings
- `ctest --test-dir build_codex_verify --output-on-failure` — **23 tests passed，0 failed**

## 总体结论

**功能正常、性能稳定、无内存泄漏、连接复用正常（99.6%）、编译干净。** 本次修复与重构未引入回退。
