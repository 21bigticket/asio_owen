# ============================================================================
# 配置已全部迁移到 config.d/ 目录下。
# Config::load() 会自动发现并加载 config.d/*.ini。
# 此文件仅作占位，可删除。
# ============================================================================
