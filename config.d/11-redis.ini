# ============================================================================
# 2. 数据库 — Redis 连接池（重启生效）
# ============================================================================
[redis]
host = 127.0.0.1
port = 6379
connect_timeout_ms = 1000
cmd_timeout_ms = 500
