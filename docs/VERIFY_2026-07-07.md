# 第三轮验证报告

> 验证日期：2026-07-07
> 验证范围：http_server.hpp 拆分、协议修复（JSON 数组值、body 超限、I/O 结果区分）、parallel_group 超时重写、chunk line 上限
> 对应优化方案：`docs/HTTP_SERVER_OPTIMIZATION_2026-07-07.md`
> 对应风险复核：acquire 跨 shard 异常清理、timer 数据竞争、chunk line 无上限

## 证据口径

本文将验证结果分为三类：

- **已复现**：本轮在当前工作区实际执行过的命令，有明确通过结果。
- **用户验收输入**：用户提供的压测输出，本文只整理其结果，不替代原始日志。
- **待补原始证据**：文档中保留为建议验证项；没有本轮原始命令输出时，不作为本轮实测结论。

## 功能压测（用户验收输入）

**环境：** VM 192.168.139.230，wrk VM 本地，30t/100c，30s × 2 轮

| 接口 | #1 RPS | #2 RPS | 平均 RPS | avg_lat | Socket Errors |
|:----|:------:|:------:|:--------:|:-------:|:-------------:|
| Health | 144,189 | 125,285 | **134,737** | 0.68ms | 0 |
| Redis | 29,172 | 32,470 | **30,821** | 3.35ms | 0 |
| MySQL | 18,181 | 19,145 | **18,663** | 5.06ms | 0 |
| Config 直连 | 16,069 | 16,166 | **16,118** | 6.22ms | 0 |
| Config 网关 | 9,943 | 9,823 | **9,883** | 9.37ms | 0 |

**结论：** 用户提供的 wrk 输出显示 10 轮全部 HTTP 200，`errors=0`。Gateway 两轮约 9.8k-9.9k RPS，数据形态稳定。

**注意：** 本节为用户提供的验收输入；原始 wrk 输出应保留在 `docs/results/` 或 CI/终端日志中，便于后续复核。

## 三向日志闭环（待补原始证据）

| 维度 | error | warn | timeout |
|:----|:----:|:----:|:-------:|
| 客户端 wrk Socket Errors | — | — | 0 |
| 上游 zebra-config（压测期间） | 0 | 0 | 0 |
| ASIO 网关 | 0 | 0 | 0 |

**状态：** 本轮未附原始日志检索命令和输出。该表应在补充上游日志、网关日志、wrk 原始输出后作为正式结论。

## 连接复用（待补原始证据）

**方法：** tcpdump 抓纯 SYN，15s 压测

| 指标 | 值 | 判定 |
|:----|:--:|:----:|
| 纯 SYN（新建连接） | 500 | — |
| 总请求 | ~105,848（15s × 7,057 RPS） | — |
| **复用率** | **99.5%** | ✅ |
| TIME_WAIT | **0** | ✅ |
| ESTAB | 77 | ✅ 稳定 |

**状态：** 本轮未附 tcpdump 命令和原始抓包统计输出。保留为建议验证项，不作为本轮已复现实测结论。

## ASan/UBSan 单测（已复现）

本轮实际执行：

```bash
cmake -B build_codex_asan -S . \
  -DCMAKE_BUILD_TYPE=Debug \
  -DCMAKE_CXX_FLAGS="-fsanitize=address,undefined" \
  -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address,undefined" \
  -DFETCHCONTENT_SOURCE_DIR_GOOGLETEST=/Users/mac/code/croot/asio_owen/build_codex_stage0/_deps/googletest-src
cmake --build build_codex_asan
ctest --test-dir build_codex_asan --output-on-failure
```

结果：**37 tests passed，0 failed**，无 sanitizer 报错。

## ASAN 长跑 RSS（待补原始证据）

| 阶段 | VmRSS |
|:----|:-----:|
| 初始 | 142,472 kB |
| Health 30s | 583,948 kB |
| Redis 30s | 642,216 kB |
| MySQL 30s | 779,636 kB |
| Config 网关 30s | 876,452 kB |

- **零运行时错误**（无 use-after-free / heap-overflow / double-free）
- **LSAN 无可报告泄漏**（stderr 为空）
- RSS 增长为 ASAN 自身 overhead，与上次一致

**状态：** 本轮只复现了 ASan+UBSan 单测；未在本轮执行 ASan 服务长跑和 RSS 采样。该表需要补充原始命令、采样间隔、stderr 文件后作为正式结论。

## Valgrind（待补原始证据）

```
LEAK SUMMARY:
    definitely lost: 0 bytes in 0 blocks    ✅
    indirectly lost: 0 bytes in 0 blocks    ✅
      possibly lost: 200 bytes in 2 blocks  ← libmysqlclient 内部初始化
    still reachable: 30,290 bytes in 587 blocks  ← 正常
ERROR SUMMARY: 2 errors from 2 contexts     ← 对应 possibly lost
```

**程序自身无泄漏。** 200 bytes `possibly lost` 来自 libmysqlclient，非代码问题。

**状态：** 本轮未执行 Valgrind；该结论依赖外部原始报告。建议将完整 Valgrind 输出保存到 `docs/results/` 后再作为验收证据。

## 风险修复复核

| # | 风险 | 修复方式 | 状态 |
|:--|:-----|:---------|:-----:|
| 1 | acquire 跨 shard 异常清理错 shard | catch 改为按 `new_conn->shard_idx` 清理实际 reserved shard，并新增 `HttpPool.FailedConnectAfterShardSkipCleansActualReservedShard` 回归测试 | ✅ 已修复 |
| 2 | timeout timer 回调数据竞争 | `read/write/resolve/connect` 改为 `parallel_group` | ✅ 已修复 |
| 3 | chunk line/trailer 无上限 | `consume_line` 加 `kMaxChunkControlLineSize = 8192`，并新增 `ProxyFraming.ChunkedControlLineTooLargeReturns502` 回归测试 | ✅ 已修复 |

## 编译与单测（已复现）

普通构建：

```bash
cmake --build build_codex_stage0
ctest --test-dir build_codex_stage0 --output-on-failure
```

结果：**37 tests passed，0 failed**。

## 总体结论

**已复现结论：** 普通构建、普通单测、ASan+UBSan 构建与单测均通过；本轮针对 acquire 跨 shard 清理、timeout 生命周期竞态、chunk control line 无上限的修复都有回归测试覆盖，未发现 sanitizer 报错。

**用户验收结论：** 用户提供的 wrk 压测输出显示全部接口正常、Socket Errors 为 0，Gateway 性能稳定。

**保守结论：** 当前证据支持“本轮已覆盖路径未发现程序自身内存泄漏或内存安全错误”。Valgrind、tcpdump 连接复用、ASan 服务长跑 RSS 仍需补充原始输出后，才能作为本轮完整验收结论。
