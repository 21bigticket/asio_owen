# ============================================================================
# 6. 安全 — IP 信任列表（SIGHUP 热加载，不停机）
# ============================================================================
# trust proxy IP list, one per line
# XFF parsing skips these from right to left
[trusted_proxies]
