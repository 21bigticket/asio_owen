# ============================================================================
# 9. 安全 — 路由黑名单（定时器热加载）
# ============================================================================
# /path = role:admin        # path with role requirement
# /path/ = role:admin       # prefix with role
# /public/path =            # blocked for all
[path_blacklist]
